import React from "react";
import getActiveListings from "../tools/activeListings";

function Listings() {
  return <></>;
}

export default Listings;

getActiveListings().then((data) => console.log(data));
